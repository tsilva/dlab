# @package _global_
defaults:
  - mnist_cnn_32_64_128_val
  - _self_

run:
  study: mnist_cnn_32_64_128_val_ls001
  question: Does lighter label smoothing improve the 32x64x128 CNN validation loss?
  hypothesis: Label smoothing 0.01 may keep calibration benefits while reducing underconfidence.

loss:
  label_smoothing: 0.01
