# @package _global_
defaults:
  - override /dataset: mnist
  - override /model: autoencoder
  - override /optimizer: adam

task: reconstruction
run:
  study: baseline
  goal: Establish a dense autoencoder reconstruction baseline on MNIST.
  tags: [baseline, autoencoder, mnist]
dataset:
  normalize: false
loss:
  name: mse
model:
  params:
    latent_dim: 32
    hidden_dims: [512, 256]
