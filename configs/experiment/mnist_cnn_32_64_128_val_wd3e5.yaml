# @package _global_
defaults:
  - mnist_cnn_32_64_128_val
  - _self_

run:
  study: mnist_cnn_32_64_128_val_wd3e5
  question: Does lighter weight decay improve the 32x64x128 CNN validation loss?
  hypothesis: Weight decay 0.00003 may reduce over-regularization while preserving some constraint on weights.

optimizer:
  weight_decay: 0.00003
