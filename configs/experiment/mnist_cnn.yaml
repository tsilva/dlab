# @package _global_
defaults:
  - override /dataset: mnist
  - override /model: cnn
  - override /optimizer: adam

task: classification
run:
  stage: 02_cnn_inductive_bias
  study: baseline
  goal: Establish a simple MNIST CNN baseline.
  tags: [baseline, cnn, mnist]
model:
  params:
    channels: [32, 64]
    dropout: 0.1
optimizer:
  lr: 0.001
