# @package _global_
defaults:
  - mnist_cnn_32_64_128_val
  - _self_

run:
  study: mnist_cnn_32_64_128_val_ls0
  question: Does removing label smoothing improve the 32x64x128 CNN validation loss?
  hypothesis: The CNN may benefit from sharper labels if label smoothing is adding unnecessary bias.

loss:
  label_smoothing: 0.0
