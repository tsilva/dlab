stages:
  - id: 01_mlp_basics
    goal: Learn how LR, width, depth, dropout, batch size, and seed affect MNIST MLPs.
    studies:
      - mlp_lr
      - mlp_lr_seed
      - mlp_arch
      - mlp_arch_confirm
      - mlp_regularization_confirm
      - mlp_regularization
      - mlp_batch_size_confirm
      - mlp_batch_lr_interaction
      - mlp_batch_lr_confirm
      - mlp_high_capacity_norm_phase1
      - mlp_high_capacity_top3_seed_confirm
      - mlp_aug_compact
      - mlp_seed

  - id: 02_cnn_inductive_bias
    goal: Compare MLPs and CNNs on image data.
    studies:
      - mnist_mlp
      - mnist_cnn
      - fashion_mnist_cnn
      - cnn_width
      - cnn_depth

  - id: 03_dataset_difficulty
    goal: See how the same model families behave as data complexity increases.
    studies:
      - mnist_mlp
      - fashion_mnist_cnn
      - cifar10_resnet18
