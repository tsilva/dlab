# @package _global_
defaults:
  - override /dataset: cifar10
  - override /model: resnet18
  - override /optimizer: adamw

task: classification
run:
  stage: 03_dataset_difficulty
  study: baseline
  goal: Establish a CIFAR-10 baseline on a standard ResNet-style model.
  tags: [baseline, resnet, cifar10]
trainer:
  max_epochs: 20
optimizer:
  lr: 0.0003
  weight_decay: 0.01
